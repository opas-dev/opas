# Runtime MDX is live

This updated article is read from disk and compiled **when the request arrives**.

> Edit this file, refresh the page, and the rendered answer changes without rebuilding OPAS.

The same compiler will receive database content in the next spike.
