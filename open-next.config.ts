// ABOUTME: Configures the OpenNext build that adapts OPAS for Cloudflare Workers.
// ABOUTME: Uses dynamic D1 reads without adding remote cache resources for the MVP.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
